import { FUITabsSize } from "../types/tabs"

const sizes: Record<FUITabsSize, string> = {
    sm: "text-xs !px-3 !h-8",
    md: "text-sm md:!text-md !px-3 md:!px-5 !h-8 md:!h-10",
}

const tabs = { sizes }

export default tabs
