import { FUISpinnerSize } from "../types/spinner"

export const sizes: Record<FUISpinnerSize, string> = {
    sm: "min-size-5 size-5",
    md: "min-size-8 size-8",
}

const spinner = { sizes }

export default spinner
